# ゲーム開始フロー接続状況

更新日: 2026-09-15

参照正本:
- `untold-wiki/pages/development/eld-opening-sequence.md`
- `untold-wiki/pages/world/eld.md`
- `untold-wiki/pages/story/protagonist.md`
- `untold-wiki/pages/story/gamemaster.md`
- `untold-wiki/pages/systems/events.md`
- `untold-wiki/pages/systems/gameplay-ui.md`

## 接続済み

- ゲーム起動直後に開発用起点選択画面を出さず、冒頭独立映像から開始する。
- 冒頭の心の声7文を正本どおりの文面で自動表示する。
- 1文約4秒というWiki上の暫定基準をコード側でも暫定値として保持する。
- 冒頭終了後、通常ワールドの開発用画面ではなくエルド図書館導入フェーズへ遷移する。
- エルド図書館を現行正本の **10×16タイル / 640×1024px** へ更新した。
- 入口、本棚、補助本棚、司書カウンター、地下階段、閲覧机、椅子の確定タイル占有を実装用データへ反映した。
- 図書館開始イベントの整理位置 `(8,1) → (5,1) → (2,1) → (3,8)` と、BOOK発見位置 `(3,5)`、BOOK位置 `(3,4)` を実装用データへ反映した。
- 各本整理時とBOOK発見時の1秒停止を反映した。
- 司書 `「いつも手伝ってくれて、ありがとう。」`、主人公 `「好きでやってるだけなんで。」` をタップ送りの会話として接続した。
- 整理④からBOOK発見位置までは、確定仕様どおり中央本棚左側の `x=2` 側を回る経路で強制移動する。
- BOOK発見時の心の声 `（ん……？ この本は……？）` と、その終了0.5秒後のBOOK取得まで接続した。
- 強制移動の表示速度は、通常移動の共通基準 `110px/s` を使用している。
- BOOK初回見開き用UIと、ゲームマスター最初のメッセージ全文は既存実装として保持している。
- BOOK初読後から司書への報告・持ち帰り許可までの確定台詞を実装用データへ反映済み。
- 開始イベントの進行順を状態として保持する `OpeningSequenceController` を保持している。

## 次の接続境界

現在の正式起動フローは

`冒頭独立映像 → 図書館開始イベント → BOOK発見・取得`

まで自動接続する。

次はBOOK取得後の初回閲覧について、確定済みの順序

`画面を少し暗くする → 閉じたBOOKを1秒表示 → タイトルページを開く → 開ききって1秒 → 主人公の心の声 → 2–3ページ → ゲームマスター全文 → 10秒入力停止`

へ既存UIを追従させる。

## まだ未確定のため補完していない項目

- BOOK初回閲覧時に画面を暗くする具体的不透明度。
- BOOKの画面内サイズ・細かな見た目。
- BOOKを開くアニメーションの具体的な所要時間。
- タイトルページから2–3ページへめくる具体的な所要時間・細かな見せ方。
- 冒頭映像終了時の暗転・フェードの具体秒数。

これらは未確定値を実装都合で固定せず、正本で確定するまで具体値を追加しない。

## CIについて

2026-09-15時点のCIでは、今回変更した図書館レイアウトテストを含む新規・既存の大半のテストは通過している。

一方、今回の変更以前から以下2件の既存テストが失敗しており、CI全体は赤のままになっている。

- `src/domain/eld-progress.test.ts` の記録から手掛かり反映に関するテスト。
- `src/game/eld-library-transition.test.ts` のNo.01取得後の退出遷移に関するテスト。

今回の変更前のコミットでも同じ2件が失敗していることを確認済みであり、今回の図書館10×16化・開始イベント接続による回帰ではない。
